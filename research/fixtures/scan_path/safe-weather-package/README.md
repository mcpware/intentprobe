# safe-weather-mcp

Returns today's weather forecast for a requested city as JSON. The server only
uses the city name supplied by the user and does not read local files, secrets,
credentials, cookies, or clipboard data.
